@using (@Html.BeginForm("Index", "Lot", FormMethod.Get, new { @class = "navbar-form navbar-right" }))
{
    <div class="form-group">
        @Html.TextBox("searchString", null, new { @class = "form-control ui-autocomplete-input", placeholder="Search...", id = "lot",})
        <button type="submit" class="btn btn-default" id="findBtn">
            <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
        </button>
    </div>
}