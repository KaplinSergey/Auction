<ul class="nav sidebar-nav navbar-collapse" id="side-menu">
    <li class="sidebar-search">
        @using (@Html.BeginForm("GetLotsList", "User", FormMethod.Get, new { @class = "navbar-form  relative" }))
        {          
            <div class="form-group">
                @Html.TextBox("searchString", null, new { @class = "form-control", placeholder = "Search...", id = "search" })
            </div>
            <button type="submit" class="btn btn-default btn-search" id="findBtn">
                <i class="glyphicon glyphicon-search"></i>
            </button>
        }
    </li>
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Lots<span class="caret"></span></a>
        <ul class="dropdown-menu">
            <li>@Html.ActionLink("Sold", "GetLotsList", "User", new { state = "Sold" }, null)</li>
            <li>@Html.ActionLink("Unsold", "GetLotsList", "User", new { state = "Unsold" }, null)</li>
            <li>@Html.ActionLink("For sale", "GetLotsList", "User", new { state = "For sale" }, null)</li>
        </ul>
    </li>
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Bids<span class="caret"></span></a>
        <ul class="dropdown-menu">
            <li>@Html.ActionLink("All", "GetBidsList", "User", new { state = "All" }, null)</li>
            <li>@Html.ActionLink("Current", "GetBidsList", "User", new { state = "Current" }, null)</li>
        </ul>
    </li>
    <li>
        @Html.ActionLink("Purchases", "GetPurchasesList", "User")
    </li>
</ul>

